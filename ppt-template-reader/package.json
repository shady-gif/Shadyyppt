{
  "scripts": {
    "build": "mkdir -p dist && cp -R web/. dist/ && node -e \"const fs=require('fs'); fs.writeFileSync('dist/config.js', 'window.PPT_API_BASE = ' + JSON.stringify(process.env.PPT_API_BASE_URL || '') + ';\\n');\""
  }
}
